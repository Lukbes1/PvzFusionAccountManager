import 'package:flutter/material.dart';

const mainShadowColor = Color(0x56000000);
const mainShadow = BoxShadow(
  color: Color.fromARGB(80, 0, 0, 0),
  blurRadius: 2,
  spreadRadius: 1,
  offset: Offset(-4, 5),
  // pushed downward
);
